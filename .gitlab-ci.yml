build:
  image: node:22
  script:
    - npm install
    - npm run lint
    - npm run format:check
    - npm run test
    - npm run build

  artifacts:
    paths:
      - dist/

  cache:
    paths:
      - node_modules
